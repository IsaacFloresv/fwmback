import { Sequelize } from "sequelize";

const db = new Sequelize('id20145964_meic_web', 'id20145964_alega_meic_web','e$ks3d~Fg1b&d%TW', {
    host: 'localhost',
    dialect: 'mysql'
    }
)

export default db